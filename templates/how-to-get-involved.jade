extends layout

block prepend title
  | #{page.title} - 

block header
  h1 How to get involved
  p.lead
    | Even small contributions make a huge difference

block content
  - var aspects = _.chain(contents["how-to-get-involved"]._.directories).map(function(item) {
  -   return item.index
  - }).compact().sortBy(function(item) {
  -   return item.metadata.order
  - }).value()

  !{ typogr(page.html).typogrify() }
